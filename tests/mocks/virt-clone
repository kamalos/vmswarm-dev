#!/usr/bin/env bash
# Mock virt-clone
echo "Mock virt-clone called with: $@"
name=""
while [[ $# -gt 0 ]]; do
  if [[ "$1" == "--name" ]]; then
    name="$2"
    break
  fi
  shift
done
mkdir -p "$(dirname "$0")/domains"
if [[ -n "$name" ]]; then
  touch "$(dirname "$0")/domains/$name"
fi
exit 0
